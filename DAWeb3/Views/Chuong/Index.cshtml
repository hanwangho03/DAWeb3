@model IEnumerable<DAWeb3.Models.Chuong>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Tên Chương
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Meta)
            </th>
            
            <th>
                Tên Môn Học
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenchuong)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Meta)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.IdMonHocNavigation.TenMonHoc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdChuong">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdChuong">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdChuong">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
